# Contributing

Keep changes small and portable.

Rules:

- Do not add framework-specific code to the core helpers.
- Put Next.js and Astro examples in `examples/`.
- Keep Sanity project IDs, dataset names, and tokens out of source code.
- Add tests for helper behavior when changing normalization, feeds, or JSON-LD.
- Prefer clear defaults over large configuration surfaces.
